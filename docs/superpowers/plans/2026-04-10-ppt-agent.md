# PPT Agent 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建 `/convert-ppt` 斜杠命令，将业务方案 PPT 自动转换为 VitePress plan.md + Slidev 文件并推送到 GitHub。

**Architecture:** 两个文件协作——Python 解析脚本负责机械性的 PPTX 解包/文字提取/图片收集，Claude Code Skill 文件负责 AI 内容理解、页面生成和用户确认交互。Skill 文件是执行主体，Python 脚本是工具。

**Tech Stack:** Python 3（标准库 zipfile/xml.etree/shutil/json）、Claude Code Skill（Markdown）、VitePress、Slidev、Git

---

## 文件结构

| 文件 | 类型 | 职责 |
|------|------|------|
| `D:/Claude/.claude/skills/convert-ppt.md` | 新建 | Skill 主文件，定义 `/convert-ppt` 的完整执行逻辑 |
| `D:/Claude/chinasofti-capability/scripts/ppt_parser.py` | 新建 | PPTX 解析器，输出结构化 JSON |

---

## Task 1：编写 PPTX 解析脚本

**Files:**
- Create: `D:/Claude/chinasofti-capability/scripts/ppt_parser.py`

### 说明

解析器接收 PPTX 路径，解压后提取每张幻灯片的文字 + 图片对应关系，输出 JSON 到 stdout。Claude 在 Skill 执行过程中通过 Bash 工具调用此脚本。

- [ ] **Step 1：创建 scripts 目录并写入解析脚本**

创建文件 `D:/Claude/chinasofti-capability/scripts/ppt_parser.py`，内容如下：

```python
#!/usr/bin/env python3
"""
PPTX 解析器 — 提取幻灯片文字、图片路径及幻灯片-图片映射关系
用法: python ppt_parser.py <path/to/file.pptx>
输出: JSON 到 stdout
"""
import sys
import os
import json
import zipfile
import shutil
import tempfile
import re
from xml.etree import ElementTree as ET

NS = {
    'a':   'http://schemas.openxmlformats.org/drawingml/2006/main',
    'r':   'http://schemas.openxmlformats.org/officeDocument/2006/relationships',
    'p':   'http://schemas.openxmlformats.org/presentationml/2006/main',
}

def extract_text(slide_xml_path):
    """从单张幻灯片 XML 提取所有文字，返回 (title, body_lines)"""
    tree = ET.parse(slide_xml_path)
    root = tree.getroot()
    texts = [t.text for t in root.iter('{http://schemas.openxmlformats.org/drawingml/2006/main}t') if t.text and t.text.strip()]
    title = texts[0] if texts else ''
    body = texts[1:] if len(texts) > 1 else []
    return title, body

def extract_image_refs(rels_xml_path):
    """从 slide_rels 文件提取本张幻灯片引用的图片文件名列表"""
    if not os.path.exists(rels_xml_path):
        return []
    tree = ET.parse(rels_xml_path)
    root = tree.getroot()
    images = []
    for rel in root:
        target = rel.get('Target', '')
        if '../media/' in target:
            images.append(os.path.basename(target))
    return images

def parse(pptx_path):
    tmp_dir = tempfile.mkdtemp(prefix='pptx_')
    try:
        with zipfile.ZipFile(pptx_path, 'r') as z:
            z.extractall(tmp_dir)

        slides_dir  = os.path.join(tmp_dir, 'ppt', 'slides')
        rels_dir    = os.path.join(slides_dir, '_rels')
        media_dir   = os.path.join(tmp_dir, 'ppt', 'media')

        # 按幻灯片序号排序
        slide_files = sorted(
            [f for f in os.listdir(slides_dir) if re.match(r'slide\d+\.xml$', f)],
            key=lambda x: int(re.search(r'\d+', x).group())
        )

        slides = []
        for sf in slide_files:
            idx   = int(re.search(r'\d+', sf).group())
            title, body = extract_text(os.path.join(slides_dir, sf))
            rels_file   = os.path.join(rels_dir, sf + '.rels')
            images      = extract_image_refs(rels_file)
            slides.append({
                'index':  idx,
                'title':  title,
                'body':   body,
                'images': images,
            })

        # 所有媒体文件
        all_images = sorted(os.listdir(media_dir)) if os.path.isdir(media_dir) else []

        result = {
            'pptx_path':  pptx_path,
            'tmp_dir':    tmp_dir,        # 调用方用完后需自行清理
            'media_dir':  media_dir,      # 图片源目录
            'slides':     slides,
            'all_images': all_images,
        }
        print(json.dumps(result, ensure_ascii=False, indent=2))
    except Exception as e:
        # tmp_dir 在出错时保留，便于调试
        print(json.dumps({'error': str(e), 'tmp_dir': tmp_dir}), file=sys.stderr)
        sys.exit(1)

if __name__ == '__main__':
    if len(sys.argv) != 2:
        print('用法: python ppt_parser.py <path/to/file.pptx>', file=sys.stderr)
        sys.exit(1)
    parse(sys.argv[1])
```

- [ ] **Step 2：验证脚本可正确解析 CMS PPT**

在终端运行（使用已知存在的 PPT 验证）：

```bash
cd "D:/Claude/chinasofti-capability"
python scripts/ppt_parser.py "D:/Claude/cms_pptx_extracted/../slidev/solutions/cms-solution.md" 2>&1 || true
# 找到实际 pptx 文件：
ls "D:/Claude/"*.pptx 2>/dev/null || find "D:/Claude" -name "*.pptx" -maxdepth 3 2>/dev/null | head -5
```

如找到 `.pptx` 文件，运行：

```bash
python scripts/ppt_parser.py "<找到的pptx路径>" | python -c "
import sys, json
d = json.load(sys.stdin)
print('幻灯片数:', len(d['slides']))
print('图片数:', len(d['all_images']))
for s in d['slides'][:3]:
    print(f'  Slide {s[\"index\"]}: {s[\"title\"]} | 图片:{s[\"images\"]}')
"
```

期望输出：幻灯片数 > 0，图片数 > 0，标题为中文。

- [ ] **Step 3：Commit**

```bash
cd "D:/Claude/chinasofti-capability"
git add scripts/ppt_parser.py
git commit -m "feat(agent): add PPTX parser script"
```

---

## Task 2：编写 Skill 文件 — 阶段一至确认点 1

**Files:**
- Create: `D:/Claude/.claude/skills/convert-ppt.md`

### 说明

Skill 文件是 Claude 读取后逐步执行的操作手册。分两个 Task 写完，本 Task 覆盖：调用解析脚本 → Claude 内容理解 → 确认点 1。

- [ ] **Step 1：创建 Skill 文件，写入 header 和阶段一**

创建 `D:/Claude/.claude/skills/convert-ppt.md`，内容如下（从文件开头到确认点 1 结束）：

````markdown
---
name: convert-ppt
description: 将业务方案 PPT 转换为 VitePress plan.md + Slidev 文件并推送 GitHub。用法：/convert-ppt D:/path/to/xxx.pptx
---

# PPT → 能力库 转换 Agent

收到 `/convert-ppt <pptx路径>` 指令后，严格按以下六个阶段执行。
**每个确认点必须等用户明确回复后才继续。**

---

## 阶段一：解析 PPTX

使用 Bash 工具运行解析脚本，将输出存入变量供后续使用：

```bash
python "D:/Claude/chinasofti-capability/scripts/ppt_parser.py" "<用户提供的pptx路径>"
```

如果脚本报错（stderr 有内容或退出码非 0），立即停止并告知用户错误信息。

解析成功后，将 JSON 结果记录到上下文，用于后续所有阶段。

---

## 阶段二：内容理解

仔细阅读 JSON 中所有幻灯片的 title 和 body 字段，然后对内容做以下分析并在脑中形成结构：

**2.1 推断模块信息**
- `module_name`：从第 1 张幻灯片标题提取中文模块名（去掉"解决方案"、"产品方案"等后缀）
- `slug`：将模块名转为小写英文 slug（如"内容分发平台"→"cms"，"用户激励"→"loyalty"，"物联网平台"→"iot"，无法判断时用拼音）
- `subtitle`：从幻灯片目录页（通常是第 1 或 2 张）提取各子系统名，用" · "连接

**2.2 识别章节结构**
遍历所有幻灯片，将内容分组为若干章节（Section），每个章节对应 plan.md 中的一个 `## 二级标题`：
- 章节标题：幻灯片的 title 字段
- 章节要点：body 字段中的功能描述
- 关联图片：该幻灯片的 images 字段

**2.3 识别架构层级**
寻找标题包含"架构"的幻灯片，分析其 body 内容，提取四层架构的各层名称和子项。如找不到，从其他幻灯片推断。

**2.4 图片语义标注**
对 all_images 列表中每张图片，结合其所在幻灯片的标题和内容，判断：
- 类型：`arch`（架构图）/ `screenshot`（功能截图）/ `skip`（封面/背景/装饰）
- 语义名：如 `live-backend`、`vote-list`、`arch` 等（用于重命名）

---

## 确认点 1 — 模块识别确认

展示以下信息，**等待用户确认后再继续**：

```
📦 检测到模块：{module_name}
   slug:        {slug}
   副标题:      {subtitle}

📁 目标路径：
   VitePress:   docs/zh/business/{slug}/plan.md
   Slidev:      slidev/solutions/{slug}-solution.md
   图片目录:    docs/public/solutions/{slug}/images/

🖼  图片匹配预览（共 {N} 张，计划保留 {M} 张）：
   arch.png           ← {原文件名}   [架构图]
   {语义名}.png       ← {原文件名}   [功能截图]
   ...
   ⊘ 跳过 {K} 张（封面/背景/装饰）

⚠️  {如 slug 已存在于 sidebar.zh.ts，追加此行：该模块已存在，将覆盖 plan.md}

✅ 确认继续？或告知需要修改的地方（slug / 图片映射 / 路径）
```

**如用户要求修改**：按其要求调整分析结果，重新展示确认点 1，直到用户确认。
````

- [ ] **Step 2：验证 Skill 文件被 Claude Code 识别**

在 Claude Code 聊天框输入 `/` 查看斜杠命令列表，确认 `convert-ppt` 出现在列表中。

如未出现，检查文件是否保存在 `D:/Claude/.claude/skills/convert-ppt.md`（路径需完全正确）。

- [ ] **Step 3：Commit**

```bash
cd "D:/Claude/chinasofti-capability"
git add D:/Claude/.claude/skills/convert-ppt.md 2>/dev/null || true
# .claude/skills 不在 repo 内，仅确认文件存在即可
ls "D:/Claude/.claude/skills/convert-ppt.md" && echo "✅ Skill 文件存在"
```

---

## Task 3：Skill 文件 — 阶段三至六（图片处理 + 生成 + 推送）

**Files:**
- Modify: `D:/Claude/.claude/skills/convert-ppt.md`（追加内容）

- [ ] **Step 1：追加阶段三——图片处理**

在 `convert-ppt.md` 末尾追加：

````markdown

---

## 阶段三：图片处理

用户确认后，执行以下操作：

**3.1 创建目标图片目录**

```bash
mkdir -p "D:/Claude/chinasofti-capability/docs/public/solutions/{slug}/images"
mkdir -p "D:/Claude/slidev/solutions/images"
```

**3.2 复制并重命名图片**

对确认点 1 中确定保留的每张图片，从 `{media_dir}` 复制到两个目标目录：

```bash
# 对每张保留的图片执行（{src} = media_dir 中的原文件名，{dst} = 语义名）
cp "{media_dir}/{src}" "D:/Claude/chinasofti-capability/docs/public/solutions/{slug}/images/{dst}"
cp "{media_dir}/{src}" "D:/Claude/slidev/solutions/images/{dst}"
```

**3.3 清理临时目录**

```bash
rm -rf "{tmp_dir}"
```

---

## 阶段四：生成 plan.md

根据内容理解结果，生成完整的 VitePress 方案页，写入：
`D:/Claude/chinasofti-capability/docs/zh/business/{slug}/plan.md`

**必须严格按照以下模板结构生成，所有 CSS 类名不得更改：**

### plan.md 模板结构

```markdown
# {module_name} · 解决方案

<div class="cms-hero">
  <div class="cms-hero-particles">
    <span></span><span></span><span></span><span></span><span></span><span></span>
  </div>
  <div class="cms-hero-content">
    <div class="cms-hero-badge">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M8 14s1.5 2 4 2 4-2 4-2"/></svg>
      Solution Overview · 解决方案概览
    </div>
    <h1>{module_name}<br><span class="cms-hero-gradient">解决方案</span></h1>
    <p>{subtitle} — {一句话定位描述}</p>
    <div class="cms-hero-tags">
      {每个子系统一个 <span> 标签，含 SVG 图标}
    </div>
  </div>
</div>

## 产品定位

<div class="cms-position-grid">
  {3 个 cms-position-card，最后一个加 cms-position-highlight class}
</div>

---

## 系统架构总览

<div class="cms-arch-container">
  {arch-layer × 4，内含 arch-tag-grid}
</div>

---

## {子系统总称，如"四大子系统"}

<div class="cms-subsystem-grid">
  {每个子系统一个 <a href="#{锚点}" class="cms-subsystem-card cms-sub-{color}">}
</div>

---

{每个子系统一个 ## 章节，含：}
## {子系统名}

<div class="cms-section-intro">...</div>

<div class="cms-feature-grid">
  {每个功能点一个 cms-feature-card，含 cms-screenshot-wrapper}
</div>

---

<div id="cms-lightbox" class="cms-lightbox" onclick="this.classList.remove('cms-lightbox-active')">
  <div class="cms-lightbox-close">&times;</div>
  <img id="cms-lightbox-img" src="" alt="" />
</div>

<script setup>
import { onMounted } from 'vue'
onMounted(() => {
  const lightbox = document.getElementById('cms-lightbox')
  const lightboxImg = document.getElementById('cms-lightbox-img')
  if (!lightbox || !lightboxImg) return
  document.querySelectorAll('.cms-screenshot-wrapper img').forEach(img => {
    img.style.cursor = 'zoom-in'
    img.addEventListener('click', () => {
      lightboxImg.src = img.src
      lightboxImg.alt = img.alt
      lightbox.classList.add('cms-lightbox-active')
    })
  })
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') lightbox.classList.remove('cms-lightbox-active')
  })
})
</script>
```

**图片路径规则：**
- VitePress plan.md 中：`/solutions/{slug}/images/{语义名}.{ext}`
- Slidev 文件中：`./images/{语义名}.{ext}`

**CSS 颜色 class 对应关系：**
- 第1个子系统：`cms-sub-blue`
- 第2个子系统：`cms-sub-purple`
- 第3个子系统：`cms-sub-cyan`
- 第4个子系统：`cms-sub-amber`
- 更多子系统循环使用上述颜色

---

## 阶段四（并行）：生成 Slidev 文件

写入：`D:/Claude/slidev/solutions/{slug}-solution.md`

参照 `D:/Claude/slidev/solutions/cms-solution.md` 的格式：
- frontmatter 包含 title、theme、transition 等
- 第一页为 cover（深色 Hero）
- 每个章节对应 1-2 张幻灯片
- 图片使用相对路径 `./images/{语义名}.{ext}`
- 图片宽度设为 100%

---

## 阶段五：注册侧边栏 + 生成占位文件

**5.1 更新 sidebar.zh.ts**

读取 `D:/Claude/chinasofti-capability/docs/.vitepress/sidebar.zh.ts`，在「业务平台」`items` 数组末尾追加：

```typescript
{
  text: '{module_name}', collapsed: true,
  items: [
    { text: '概览', link: '/zh/business/{slug}/' },
    { text: '方案', link: '/zh/business/{slug}/plan' },
    { text: '报价', link: '/zh/business/{slug}/pricing' },
    { text: 'DEMO',  link: '/zh/business/{slug}/demo' },
  ]
},
```

如该 slug 已存在，跳过此步（plan.md 已覆盖，sidebar 无需重复添加）。

**5.2 生成占位 md 文件（如不存在）**

```bash
mkdir -p "D:/Claude/chinasofti-capability/docs/zh/business/{slug}"

# index.md（如不存在）
[ ! -f "D:/Claude/chinasofti-capability/docs/zh/business/{slug}/index.md" ] && \
  echo "# {module_name}" > "D:/Claude/chinasofti-capability/docs/zh/business/{slug}/index.md"

# pricing.md（如不存在）
[ ! -f "D:/Claude/chinasofti-capability/docs/zh/business/{slug}/pricing.md" ] && \
  echo "# 报价" > "D:/Claude/chinasofti-capability/docs/zh/business/{slug}/pricing.md"

# demo.md（如不存在）
[ ! -f "D:/Claude/chinasofti-capability/docs/zh/business/{slug}/demo.md" ] && \
  echo "# DEMO" > "D:/Claude/chinasofti-capability/docs/zh/business/{slug}/demo.md"
```

---

## 确认点 3 — 生成摘要确认

**等待用户确认后再执行 push：**

```
✨ 生成完成，摘要：

   plan.md     — {N} 个章节，{M} 张截图，HTML 架构图（{层数}层）
   Slidev      — {K} 张幻灯片
   sidebar     — {已追加新条目 / slug 已存在，跳过}
   占位文件    — index.md / pricing.md / demo.md {已生成 / 已存在}

🚀 确认 push 到 GitHub？
```

---

## 阶段六：构建 + Git 推送

用户确认后执行：

```bash
cd "D:/Claude/chinasofti-capability"

# 构建验证
npx vitepress build docs

# 如 build 失败，停止并报告错误，不执行后续步骤

# Git 推送
git add \
  "docs/zh/business/{slug}/" \
  "docs/public/solutions/{slug}/" \
  "docs/.vitepress/sidebar.zh.ts"

git commit -m "feat({slug}): add solution page converted from PPT"
git push origin master
```

推送成功后输出：

```
🎉 完成！
   已推送到 GitHub: chenxxiang/chinasofti-capability
   页面地址: /zh/business/{slug}/plan
```
````

- [ ] **Step 2：Commit Skill 文件最终版**

```bash
# Skill 文件在 .claude 目录，不在 git repo 内，验证文件完整性即可
wc -l "D:/Claude/.claude/skills/convert-ppt.md"
# 期望：> 150 行
echo "✅ Skill 文件完整"

# 提交 repo 内的变更
cd "D:/Claude/chinasofti-capability"
git add docs/superpowers/plans/2026-04-10-ppt-agent.md
git commit -m "docs: add PPT agent implementation plan"
```

---

## Task 4：端到端测试

**Files:**
- Test target: `D:/Claude/.claude/skills/convert-ppt.md`

使用一个**真实的新 PPT 文件**（不是 CMS，避免覆盖现有内容）执行完整流程，验证所有阶段正常工作。

- [ ] **Step 1：准备测试 PPT**

找一个已有的业务方案 PPT（非 CMS）：

```bash
find "D:/Claude" -name "*.pptx" -maxdepth 4 2>/dev/null
```

如找到多个，选一个最简单的（幻灯片数少的）作为测试对象。

- [ ] **Step 2：执行转换**

在 Claude Code 中输入：

```
/convert-ppt D:/path/to/test.pptx
```

按照三个确认点正常交互，完成完整流程。

- [ ] **Step 3：验证输出**

```bash
cd "D:/Claude/chinasofti-capability"

# 检查文件生成
ls docs/zh/business/{slug}/
# 期望: index.md plan.md pricing.md demo.md

ls docs/public/solutions/{slug}/images/
# 期望: 至少 1 张图片

ls "D:/Claude/slidev/solutions/{slug}-solution.md"
# 期望: 文件存在

# 检查侧边栏
grep -n "{slug}" docs/.vitepress/sidebar.zh.ts
# 期望: 找到对应条目

# 本地预览
npx vitepress dev docs --port 5174
# 浏览器打开 http://localhost:5174/zh/business/{slug}/plan 确认页面正常渲染
```

- [ ] **Step 4：验证 Slidev 图片加载**

```bash
cd "D:/Claude/slidev"
npm run dev -- solutions/{slug}-solution.md --port 3031
# 浏览器打开 http://localhost:3031 确认图片正常显示
```

- [ ] **Step 5：如测试通过，执行最终 push**

在确认点 3 选择确认，Agent 自动完成 build + push。

---

## Self-Review 检查

| 规格要求                      | 对应 Task             |
| ------------------------- | ------------------- |
| PPTX 解析（文字 + 图片 + 映射）     | Task 1              |
| 内容理解（模块名/slug/章节/图片语义）    | Task 2（Skill 阶段二）   |
| 确认点 1（模块识别）               | Task 2（Skill 确认点 1） |
| 图片复制 + 重命名到两个目录           | Task 3（阶段三）         |
| plan.md 生成（含 lightbox JS） | Task 3（阶段四）         |
| Slidev 文件生成               | Task 3（阶段四并行）       |
| 侧边栏更新 + 占位文件              | Task 3（阶段五）         |
| 确认点 3（push 前预览）           | Task 3（确认点 3）       |
| VitePress build 验证        | Task 3（阶段六）         |
| Git push                  | Task 3（阶段六）         |
| 错误处理（build 失败停止 push）     | Task 3（阶段六备注）       |
| slug 已存在提示                | Task 2（确认点 1）       |
| 端到端测试                     | Task 4              |

> 确认点 2 已合并入确认点 1 展示（图片匹配预览在同一屏），减少打断次数，比规格更优。
